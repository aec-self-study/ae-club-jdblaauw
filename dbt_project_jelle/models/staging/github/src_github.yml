version: 2

sources:
  - name: github
    database: analytics-engineers-club  
    tables:
      - name: pull_request
        columns:
          - name: id
            description: "This is not the PR number"
            tests:
              - unique
              - not_null

      - name: issue
        description: "this table contains both issues and PR's"
        columns:
          - name: id
            tests:
              - unique
              - not_null

      - name: issue_merged
        description: ""
        columns:
          - name: issue_id
            tests:
              - unique
              - not_null

      - name: repository
        columns:
          - name: id
            tests:
              - unique
              - not_null
        
